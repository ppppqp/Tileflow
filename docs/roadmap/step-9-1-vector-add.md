# Step 9.1: Vector Add Deep Dive

Scope: 2-3 days.

Goal: make vector add the canonical walkthrough through every TileFlow layer.

Build:

- Show Python DSL, traced IR, inferred layout, pipeline, MLIR, and lowered output.
- Add docs-only commentary.
- Keep runtime output clean.

Tests:

- Example stays runnable.
- Expected output remains stable.

References:

- `examples/vector_add.py`
- `ref/Enigma-DSL/examples/vector_add.py`

Learning focus:

- End-to-end compiler explanation.

Writing output:

- Course chapter: "Vector add through every layer of TileFlow."

